@model MVC5.Controllers.FakeModelView
    @{
    ViewBag.Title = "MultiSelect";
  //  string[] strs = { "nissan", "ford", "subaru" };
    }

    <h2>MultiSelect</h2>

    <div class="form-horizontal">
        <form method="get">
           
                <div class="col-md-12">
                    @Html.ListBoxFor(m => m.Cars,
         new MultiSelectList(Model.AllCars)
         , new { @class = "form-control", data_placeholder = "Производители..." })
                </div>
            <hr/>
            <button class="btn btn-lg btn-primary" type="submit"><span class="glyphicon glyphicon-search"></span> Найти</button>
        </form>
    </div>
<div class="row">
    @using (Html.BeginForm())
{
    @Html.ValidationSummary()
    @Html.TextBoxFor(p=>p.CarName)
    <button class ="btn" type="submit">asdasd</button>
    }
    </div>

@section scripts {
   <script>

       $(function () {
           $('select').chosen();
       });
       </script>
    }